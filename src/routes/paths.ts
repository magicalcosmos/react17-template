const CommonPath = {
  // faq
  FAQ: '/faq',
  // 关于工具
  About: '/about',
  // 修改密码
  ChangePassword: '/changePassword',
  // 退出登录
  Signout: 'signout'
};

const AdminPath = {
};

const TesterPath = {
};
export { CommonPath, AdminPath, TesterPath };
